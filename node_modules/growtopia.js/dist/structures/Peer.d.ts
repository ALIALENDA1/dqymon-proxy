import type { Host, NativePeer } from "../../native";
import type { PeerData, Sendable } from "../../types";
import { Client } from "./Client";
export declare class Peer<T extends PeerData> {
    private client;
    data: T;
    constructor(client: Client, netID: number, channelID?: number);
    get native(): NativePeer;
    /**
     * Sends multiple packets to a single peer.
     * @param data An argument of packets that contains the `parse()` function or just an array of Buffers.
     */
    send(...data: Sendable[]): void;
    /**
     * Sends multiple packets to a single peer.
     * @param _client The Wrapper client.
     * @param netID The netID of a peer.
     * @param channelID The channelID of a channels.
     * @param data An argument of packets that contains the `parse()` function or just an array of Buffers.
     */
    static send(_client: Host, netID: number, channelID: number, ...data: Sendable[]): void;
    /**
     * Disconnects the peer.
     * @param type Type of disconnection. Defaults to `later`.
     */
    disconnect(type?: "now" | "later" | "normal"): void;
}
